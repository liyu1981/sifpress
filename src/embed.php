/*
 * The regions between the markers below are replaced wholesale by
 * build.php (via preg_replace_callback, so no $/backslash munging).
 * This keeps the build idempotent: it works on the unborn template
 * and on an already-built dist/index.php alike.
 */
// ___BEGIN_EMBEDDED___
const EMBEDDED_HTML = '';
// ___END_EMBEDDED___
// ___BEGIN_UI_SDK___
const UI_SDK_JS = '';
// ___END_UI_SDK___